python-telegram-bot[job-queue]>=21.0,<22.0
litellm>=1.38.1
python-dotenv>=1.0.0
sqlalchemy[asyncio]>=2.0.0 # Ensure asyncio support is installed
aiosqlite>=0.19.0 # Async driver for SQLite
psycopg2-binary # Sync driver for PostgreSQL (keep if needed elsewhere, or use asyncpg)
# asyncpg # Async driver for PostgreSQL (uncomment if using Postgres async)
apscheduler>=3.10.0
