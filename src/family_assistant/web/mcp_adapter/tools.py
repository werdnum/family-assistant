"""Tools the MCP adapter exposes. Placeholder until implemented."""

from mcp.server.fastmcp import FastMCP


def register_tools(mcp: FastMCP) -> None:
    """Register the adapter's tools on ``mcp``."""
    del mcp
