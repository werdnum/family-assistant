apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - secrets.yaml
  - dev-pod.yaml
  - network-policy.yaml
  - resource-quota.yaml
namespace: family-assistant-dev
# Optional: Set the image dynamically
images:
  - name: containers.andrewgarrett.dev/family-assistant-devcontainer
    newTag: latest
