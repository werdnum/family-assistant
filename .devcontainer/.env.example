# Docker Compose Environment Variables for Family Assistant Development Container

# Directory Configuration
# These control where volumes are mounted on your host system
WORKSPACE_DIR=./workspace           # Directory for the project workspace
CLAUDE_HOME_DIR=./claude-home       # Directory for Claude's home (settings, cache, etc.)

# GitHub Configuration
CLAUDE_PROJECT_REPO=https://github.com/werdnum/family-assistant.git
GITHUB_TOKEN=                       # GitHub personal access token (for private repos)

# MCP Server API Keys (optional)
BRAVE_API_KEY=                      # Brave Search API key for web search
HOMEASSISTANT_API_KEY=              # Home Assistant API key for home automation
GOOGLE_MAPS_API_KEY=                # Google Maps API key for location services

# Family Assistant Specific (add as needed)
# Add any other environment variables your family-assistant app needs
# These will be passed through to both the backend and claude containers